bank-id: hsbc

bank {
  accounts: [
    {
      name: "HSBC 5600123702, VND"
      bic: HSBCSGSGXXX
      number: 5600123702
      currency: VND
    },
    {
      name: "HSBC 5600123703, SGD"
      bic: HSBCSGSGXXX
      number: 5600123703
      currency: SGD
    },
  ]
}

tests {
  blacklist: [
    //"io.token.InstantPaymentTest.instantPayment_debitFailure", // HIPN-71,  Over the balance
    "io.token.AccessTokenTest.accountAccess_getTransactions",    // HIPN-162, GetTransactions, order is wrong
                                                                 // HIPN-173, Same transaction id for different transfers
    "io.token.TransactionsTest.testBalanceUpdate",               // HIPN-165, Balance not being updated, needs core banking integration
    //"io.token.TransactionsTest.getTransaction_wrongAccount",   // HIPN-164, GetTransaction, need to check the account
    //"io.token.TransactionsTest.getTransaction_wrongId",        // HIPN-163, GetTransaction, fails with UNKNOWN if id is invalid
    //"io.token.TransactionsTest.getTransactions",               // HIPN-162, GetTransactions, order is wrong
                                                                 // HIPN-173, Same transaction id for different transfers
    //"io.token.TransactionsTest.getTransactions_paged",         // HIPN-162, GetTransactions, order is wrong
                                                                 // HIPN-173, Same transaction id for different transfers
    "io.token.TransferRedemptionTest.redeemToken_failure",       // HIPN-71,  Over the balance
  ]
}
