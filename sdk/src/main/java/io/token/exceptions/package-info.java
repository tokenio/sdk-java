/**
 * Defines cross-cutting Token API exception types that must be handled by all Token API callers.
 * These exceptions, when thrown, are either fatal or require user input to proceed.
 */
package io.token.exceptions;